#rentas_destacadas.px-6.w-full
  p(class="text-3xl text-primary font-semibold pb-6") Trending Stays 
  div(class="w-full h-full flex flex-col space-y-6 items-center justify-center")
    each value in card_rentas 
      +Card--rentas(value[0],value[1],value[2])